# Runtime dependencies
#
# requests — required, the download core won't work without it.
# pywebview — recommended. Without it the app still runs, but opens
# in a regular browser (not a bug, a fallback).

requests>=2.28,<3
pywebview>=6.2.1,<7
