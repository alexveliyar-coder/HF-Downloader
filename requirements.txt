# Runtime dependencies
#
# requests — обязательная, ядро скачивания без неё не работает.
# pywebview — рекомендуемая. Без неё программа запустится, но откроется
# в обычном браузере (это не баг, а запасной вариант).

requests>=2.28,<3
pywebview>=4.4,<6
